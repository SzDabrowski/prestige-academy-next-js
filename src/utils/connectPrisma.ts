// src/utils/connectPrisma.ts
// NOTE: MongoDB/Prisma is no longer used. This module is kept as a stub
// so existing imports don't break, but it will not attempt a DB connection.

const prisma = null as any;

export default prisma;
